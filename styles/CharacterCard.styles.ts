import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  box: {
    width: "80%",
    alignItems: "center",
    padding: 10,
    borderRadius: 10,
  },
  image: {
    height: 100,
    width: 100,
    borderRadius: 100,
    gap: 8,
  },
});

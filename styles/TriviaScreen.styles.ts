import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    width: 300,
    height: 60,
    justifyContent: "center",
    alignItems: "center",
  },
  buttonText: {
    textAlign: "center",
    flexShrink: 1,
  },
  image: {
    height: 200,
    width: 200,
  },
});
